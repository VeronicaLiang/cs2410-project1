// filename is the filename of benchmark
filename=/Users/Rokeer/Documents/Works/Github/cs2410-project1/benchmark1.dat

// The maximum number of instructions can be fetched in one cycle
NF=4

// The length of the instruction queue
NQ=8

// The maximum number of instructions can be decoded in one cycle
ND=4

// The length of the Decoded instruction queue
NI=8

// The maximum number of instructions can be issued to reservation stations
NW=4

// Size of ROB
NR=16

// busses connecting execution stations to ROB
NB=4

// Size of CDB
NC=4

// dump all the values in each register at the end of the simulation
dump_regs=1

// dump all the values in a range of memory locations specified
dump_mem=1
dump_start=200
dump_end=400

// dump content of branch prediction buffer at the end of simualtion
dump_branch=1

// execute the program step by step
stepbystep=0

// using for demostration, it will dump too much information
// please set it to 0 when grading, because it will replace other dump requests
demo=1